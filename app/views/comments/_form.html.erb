<%= form_for :comment, url: comments_path(article_id: @article.id),
method: :post do |f| %>
  <%= f.label :author_name %>
  <%= f.text_field :author_name %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.submit %>
  <% end %>
